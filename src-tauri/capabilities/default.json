{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacidades mínimas: core (ventana, eventos, app) + sql:default para que la WebView pueda leer y escribir la DB local. No fs, no shell, no http. Ver design.md §2 'Capacidades mínimas'.",
  "windows": ["main"],
  "permissions": ["core:default", "sql:default", "sql:allow-execute", "sql:allow-select"]
}
