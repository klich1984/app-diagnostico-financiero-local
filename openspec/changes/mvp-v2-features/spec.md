# Spec canónico

Este archivo ya no es la fuente de verdad del cambio `mvp-v2-features`.

La especificación canónica reconocida por el flujo SDD nativo está en:

```txt
openspec/changes/mvp-v2-features/specs/mvp-v2-features/spec.md
```

Mantener este puntero evita dos fuentes de verdad mientras conserva contexto para lectores que abran el antiguo `spec.md` plano.
